/**
 * This module handles environment variables.
 * Currently it's empty but serves as a placeholder for future configuration.
 */

export const env = {
  // Add environment variables here as needed
  // Example: VITE_API_URL: import.meta.env.VITE_API_URL,
};
